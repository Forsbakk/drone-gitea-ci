kind: pipeline
name: default

steps:
- name: build
  image: alpine
  environment:
    VCENTER_SERVER:
      from_secret: vcenter_server
    PASSWORD:
      from_secret: vcenter_password
  commands:
    - echo $VCENTER_SERVER
